#Example .fspy script to test error and warning surfacing and logging.

#-----------------------------------------------------------------

#Initialize

lk.
declare A,B:bool.
declare axA: (A).
declare axAB: (A->B).
#Next line should result in syntax error.
#testing.
#This should prompt the user to specify a name/body.
#theorem test (A).
theorem test : (A).
#This should inform the user that there is nothing to elim:
#elim.
#This should prompt the user to work some more:
#axiom.
#This should prompt the user to specify a proposition.
cut X (A->B).
